# Supabase Configuration
# ---------------------
# Replace these values with your own Supabase project credentials
# You can find these in your Supabase dashboard under Project Settings > API

# The URL of your Supabase project (required)
REACT_APP_SUPABASE_URL=https://your-project-id.supabase.co

# Your Supabase anon/public key (required)
# This is safe to use in browser code - it has limited permissions
REACT_APP_SUPABASE_ANON_KEY=your-anon-key-here

# Instructions:
# 1. Copy this file and rename it to .env
# 2. Replace the placeholder values with your actual Supabase credentials
# 3. Restart your development server after creating/updating the .env file

# Note: Files starting with a dot (.) are hidden by default in most file explorers.
# You may need to enable "Show hidden files" to see the .env file after creation. 